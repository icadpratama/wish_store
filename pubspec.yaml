name: wish_store
description: Developing a platform that allows users to post photos or a description of goods they need. Then, based on this data, various sellers could offer goods or custom manufacturing services.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true